# 压缩后的组件效果说明

## 结论

本分支压缩的是 Provider 模板的运行时组件树。原 .cardtpl 不改；新增的
compressed-provider-component-dag.json 保存压缩后的共享 DAG，供人直接查看。

当前全量产物包含 82 个模板入口。压缩前有 841 个节点出现位置，压缩后有 417 个唯一节点，减少 424 个
重复引用，节点对象减少 **50.4%**。

Battery 是最明显的例子：16 个模板原有 131 个节点出现位置，压缩后为 37 个唯一节点，减少 94 个，
节点对象减少 **71.8%**。

## 压缩的颗粒度

主要压缩的是 **L3 受控模板形态内部的 TemplateNode 树**。例如
BatteryOverviewNormalFull@1、BatteryOverviewChargingFull@1 和
BatteryOverviewLowFull@1 仍是三个不同的 L3 模板入口；它们的数据契约、检索语义和模板 ID 不会合并。

被共享的是这些 L3 模板内部由 Column、Row、Stack、Text、Progress、条件节点和 Image
组成的原子 UI 子树。若整个 L3 模板体完全相同，根节点也可以共享。

不会把 L2 业务语义（例如 BatteryOverview）合并成别的业务，也不会把不同卡片布局强行折叠为一个
万能组件。10 个 Layout Provider 模板同样进入分析范围，但本次主要收益来自业务 L3 模板的重复子树。

## Battery：压缩前的真实 CardTPL

下面三个入口来自同一个
resources/source/providers/battery/templates/battery-overview.cardtpl。它们的模板名不同，但模板体完全
相同。

~~~cardtpl
#Template BatteryOverviewNormalFull@1(props: { batteryIcon?: asset })
data = {
    percent: $path("/batterySOC"),
    percentText: $path("/batterySOCText"),
    charging: $path("/chargingStatusDesc"),
    level: $path("/batteryCapacityLevelDesc")
}

Column(
  "compact",
  { "_advancedComponent": "BatteryOverview", "width": "matchParent" },
  Text("设备电量", "subtitle", { "fontSize": 16, "fontWeight": 700 }),
  Text(\`电量 \${data.percentText}，\${data.level}，\${data.charging}\`, "body", { "fontSize": 12 }),
  Stack(
    "overlay",
    { "width": "matchParent", "layoutWeight": 1 },
    Stack(
      "overlay",
      { "width": 52, "height": 52, "alignContent": "center" },
      Progress({ "value": data.percent, "total": 100, "type": "ring", "strokeWidth": 6 }),
      IfPresent(props.batteryIcon, Image(props.batteryIcon, "icon", { "width": 24, "height": 24 }))
    )
  )
)
#End
~~~

~~~cardtpl
#Template BatteryOverviewChargingFull@1(props: { batteryIcon?: asset })
#Template BatteryOverviewLowFull@1(props: { batteryIcon?: asset })

# 两者的 data 声明和 Column(...) 模板体均与 NormalFull 完全相同。
# 原文件会分别重复保存这一棵树。
~~~

为保证示例可读，上面省略了不影响比较的颜色、截断和约束属性；原文件保留这些属性，压缩判断也会完整比较
它们，而不是只比较这段简写。

## Battery：压缩后实际落盘的 DAG

压缩后的正式产物不是新的 cardtpl/1 语法，而是
compressed-provider-component-dag/1 JSON。原因是现有编译器只接受原 cardtpl/1，不能把检查快照
误当成可编辑输入。

以下是全量 JSON 中三个真实模板入口的等价结构；它们都指向同一个根节点：

~~~json
{
  "templateId": "BatteryOverviewNormalFull",
  "variants": [{ "rootNodeId": "2cbdf5eeeebb..." }]
}
{
  "templateId": "BatteryOverviewChargingFull",
  "variants": [{ "rootNodeId": "2cbdf5eeeebb..." }]
}
{
  "templateId": "BatteryOverviewLowFull",
  "variants": [{ "rootNodeId": "2cbdf5eeeebb..." }]
}
~~~

该根节点及其子节点在同一 JSON 的 nodes 表中只存一次：

~~~text
2cbdf5eeeebb  Column
├── 9a0f50fe62f2  Text("设备电量")
├── d595a503924a  Text("电量 \${...}")
└── 68f3fe14d194  Stack
    └── 4dea8b179eb6  Stack
        ├── b10e6cf6787e  Progress(value = percent, total = 100, type = ring)
        └── 770f91704a72  IfParam(batteryIcon)
            └── caaa5566c470  Image(batteryIcon)
~~~

这就是“压缩成什么”：不是把三张电池卡变成一张卡，而是三个模板入口共同引用同一份 Column → Stack →
Progress/Image 树。编译时沿 ID 展开，最终 Form 原子组件树与原始 .cardtpl 相同。

## 同一结果的 TPL 式引用视图

为了让习惯看 TPL 的人一眼看懂，下列是上面 JSON DAG 的可读等价视图。Node 和 Ref 是**说明符号**，
不是现有 cardtpl/1 语法，不能直接交给 Provider 编译器。

~~~text
#Node 2cbdf5eeeebb = Column(
  "compact",
  { "_advancedComponent": "BatteryOverview", "width": "matchParent", ... },
  Ref(9a0f50fe62f2),  # Text("设备电量")
  Ref(d595a503924a),  # Text("电量" + data.percentText + data.level + data.charging)
  Ref(68f3fe14d194)
)

#Node 68f3fe14d194 = Stack(
  "overlay",
  { "width": "matchParent", "layoutWeight": 1, ... },
  Ref(4dea8b179eb6)
)

#Node 4dea8b179eb6 = Stack(
  "overlay",
  { "width": 52, "height": 52, "alignContent": "center", ... },
  Ref(b10e6cf6787e),  # Progress(value = data.percent, total = 100, type = "ring")
  Ref(770f91704a72)   # IfParam(batteryIcon) -> Image(batteryIcon)
)

#Template BatteryOverviewNormalFull@1   = Ref(2cbdf5eeeebb)
#Template BatteryOverviewChargingFull@1 = Ref(2cbdf5eeeebb)
#Template BatteryOverviewLowFull@1      = Ref(2cbdf5eeeebb)
~~~

换句话说，原来三个 TPL 文件入口各自内联一整棵 Column 树；压缩后只有一份 Node 定义，三个入口只保留
一行 Ref。实际落盘 JSON 用 rootNodeId 和 childNodeIds 表示同一件事。

## 属性与条件：规则有多严

正式压缩只接受**完全一致**的节点。节点摘要同时包含：

- 组件类型；
- 全部属性值，包含文字、颜色、尺寸、绑定、参数、对象和数组；
- 子节点的顺序与子节点摘要；
- spreadChildren；
- 条件节点本身及其条件参数。

因此，fontSize: 12 与 fontSize: 14、data.percent 与 data.level、IfParam(batteryIcon) 与
IfParam(otherIcon)、子节点调换顺序，都会形成不同节点，绝不会在正式 Registry 中共用。

代码也会在复用前再次比较完整节点值；摘要碰撞或值不等会直接失败。

近似结构分析相对宽一些，但**只写入候选报告，不注册、不渲染**。它只允许同类型的字面量、参数或绑定差异；
值类型和 required 状态必须一致。结构差异比例最多 25%，属性差异比例最多 40%，且不会把 Button、
Checkbox、IconAction 或 PillAction 替换成别的组件。当前运行链路没有采用任何近似候选。

## 复现

~~~bash
PYTHONPATH=widget_service/cloud python3 \
  widget_service/cloud/services/template_generation/compression_cli.py \
  --write-compressed-dag \
  widget_service/cloud/services/template_generation/engine/cardplan/generated/\
compressed-provider-component-dag.json
~~~

生成后搜索 BatteryOverview，即可从模板入口跟随 rootNodeId 和 childNodeIds 查看完整压缩结构。
