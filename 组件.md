找到了：你说的 70–90 个，当前实现准确是 **82 个高级组件定义**。

| 颗粒度 | 数量 | 存放位置 | 含义 |
|---|---:|---|---|
| 原子 Form 组件 | 10 | `skills/.../references/protocol/component-catalog.md` | Text、Image、Row、Column 等最终渲染积木 |
| 业务高级组件 | 11 组 / 72 个具体形态 | `widget_service/cloud/services/template_generation/resources/source/providers/*/provider.json` | 如天气、电池、耳机；同一业务按状态、尺寸、视觉形态拆成多个模板 |
| 布局高级组件 | 10 | `.../providers/layout/provider.json` | 如 HeroActionLayout、PeerPairLayout，负责卡片骨架和动作区域 |
| 合计高级组件形态 | **82** | 上述 Provider 定义 | 72 个业务形态 + 10 个布局骨架 |

业务形态的分布是：天气 9、日历 10、电池 16、应用使用时长 4、倒计时 1、耳机 14、健康运动 16、系统内存 2；共 72。它们实际的组件树不逐个放成 72 个文件，而是由各 Provider 的 `provider.json` 条目指向对应的 `.cardtpl` 文件；目前是 9 个源模板文件承载这 82 个定义。

以电池举例：

```text
BatteryOverview（业务语义，较粗）
 ├─ BatteryOverviewNormalFull@1
 ├─ BatteryOverviewChargingFull@1
 ├─ BatteryOverviewLowFull@1
 ├─ BatteryOverviewNormalCompact@1
 └─ …共 16 个具体可选形态（较细）
```

生成时根节点再选一个布局高级组件，例如：

```text
HeroSupportActionLayout
 ├─ BatteryOverviewChargingCompact@1
 ├─ WeatherOverviewCompact@1
 └─ Action
```

所以“组件库不同颗粒度”在这里确实是分层的：原子渲染组件 < 业务内容组件/具体模板形态 < 卡片布局组件。业务分组不是另外手工维护的一张清单，而是服务启动时从 Provider 的模板条目自动归纳出来；代码在 [registry.py](/Users/simonhcb/Desktop/CreateMyCard/widget_service/cloud/services/template_generation/engine/cardplan/registry.py:124) 中完成加载和聚合。

另有两个容易混淆的旧/辅助资产：

- [advanced-component-registry.json](/Users/simonhcb/Desktop/CreateMyCard/widget_service/cloud/services/template_generation/resources/source/advanced-component-registry.json) 只有 15 个较粗的旧式组件定义。
- [template-registry.json](/Users/simonhcb/Desktop/CreateMyCard/widget_service/cloud/services/template_generation/resources/source/template-registry.json) 有 40 个较细的历史通用模板。

当前 Provider 化的主组件库应优先看 `resources/source/providers/`；迁移说明也明确记录了“拆分为 82 个模板 ID 即 UI 形态”的结果：[migration-notes.md](/Users/simonhcb/Desktop/CreateMyCard/widget_service/cloud/services/template_generation/docs/migration-notes.md:10)。本次仅阅读与统计，未修改文件。