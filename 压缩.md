核心方案：

```text
82 棵模板 DOM Tree
→ 规范化为带属性的有序树
→ 发现完全相同子树
→ 发现结构相似子树并抽取差异参数
→ 从候选公共子图中选择最划算的一组
→ 形成无环的可复用组件 DAG
→ 82 个原模板 ID 变成很薄的装配入口
→ 展开后仍得到原来的 Form 原子组件树
```

这里压缩的是云侧模板资产，不改变端侧最终展开的 10 种原子 Form 组件。

## 一、问题建模

每个高级组件模板都表示为一棵有序属性树：

\[
T=(V,E,A)
\]

其中：

- \(V\)：DOM 节点，例如 `Column`、`Row`、`Text`、`Progress`。
- \(E\)：父子关系；因为 UI 子节点有渲染顺序，所以是有序边。
- \(A(v)\)：节点属性，包括样式、文本、数据绑定、素材引用、条件节点等。

例如电池环形区域可以抽象为：

```text
Stack
├── Progress
│   ├── value = Bind("/batterySOC")
│   ├── total = 100
│   ├── type = "ring"
│   └── color = "#FFF9A01E"
└── IfPresent(batteryIcon)
    └── Image
        ├── src = batteryIcon
        └── size = 24 × 24
```

不同模板可能只有以下差异：

- 数据路径不同；
- 固定文案不同；
- 颜色不同；
- 宽高、字号不同；
- 某个节点存在或不存在；
- children 数量不同；
- 节点顺序略有变化；
- 外层布局不同，但内部业务块相同。

因此不能只做文本去重，也不能只比较 JSON 是否完全相等，需要做“结构相似 + 属性差异参数化”。

---

## 二、整体算法：约束型公共子树抽取

算法分为六个阶段。

### 1. 统一解析为 IR

先把 `provider.json` 和 `.cardtpl` 中真正参与渲染的部分统一解析为中间表示。

节点类型可以设计为：

```text
Element(componentType, attributes, children)
Conditional(conditionType, conditionValue, child)
Binding(path, valueType)
Literal(value, valueType)
Parameter(name, valueType)
Expression(expressionAST)
```

例如：

```text
Text(
  `电量 ${data.percentText}`,
  "body",
  {"fontSize": 12}
)
```

规范化后：

```text
Element(
  type = Text,
  attrs = {
    content: Concat(
      Literal("电量 "),
      Binding("/batterySOCText")
    ),
    semanticStyle: "body",
    styles.fontSize: Literal(12)
  }
)
```

`provider.json` 中的数据要求、业务归属、尺寸形态等不混入 DOM Tree，而是作为模板外部契约保存：

```text
TemplateContract {
    templateId
    businessId
    sizeClass
    primaryData
    secondaryData
    optionalData
    actionPolicy
}
```

这一步很重要：**DOM 可以压缩，准入契约不能因为压缩而丢失。**

### 2. 规范化

同一结构可能因为写法不同而无法直接比较，所以需要先规范化。

主要规则包括：

- 对象属性按照固定顺序排列；
- 默认值统一为“显式默认”或“全部省略”，只能选一种；
- 颜色统一格式；
- 数字单位统一；
- 数据路径转成带类型的绑定节点；
- `IfPresent(data.x)` 和内部编译形式统一为同一种条件节点；
- 文本插值解析成表达式树；
- 不影响语义的格式、空行和字段顺序全部消除；
- 保留 children 顺序；
- 生成阶段产生的临时 ID 不参与相似度计算。

规范化后，两个节点的身份不再依赖源代码排版。

---

## 三、第一层：完全相同子树压缩

先做最安全的精确压缩。

对每个节点自底向上计算结构哈希：

\[
H(v)=Hash(
  nodeType(v),
  CanonicalAttributes(v),
  H(child_1),\dots,H(child_n)
)
\]

两个节点哈希相同且深度比较也相同，就说明它们是完全一样的子树。

把所有相同子树合并后，森林会变成一个 DAG：

```text
Template A ─┐
Template B ─┼─> SharedSubtree X
Template C ─┘
```

这一步相当于 DOM 的 hash-consing，时间复杂度近似：

\[
O(N)
\]

其中 \(N\) 是 82 个模板的总节点数。

在本项目中，电池的 `NormalFull`、`ChargingFull`、`LowFull` 当前就有大段完全相同的树；这类重复可以直接抽取，不需要任何模糊判断。

但不能把所有两三个节点的小子树都抽成组件，否则会产生大量没有维护价值的碎片。因此需要设置收益函数：

\[
Saving(S)
=
occurrence(S)\times cost(S)
-
definitionCost(S)
-
referenceCost(S)
\]

只有 `Saving(S) > threshold` 才提取。

---

## 四、第二层：相似子树参数化

精确去重之后，剩余的主要是“骨架一样，少量属性不同”。

这里使用树的反统一，也就是 Least General Generalization。

例如两个子树：

```text
Progress(value=A, color="#FF00AA00", width=52)
Progress(value=B, color="#FFFF0000", width=44)
```

计算出的公共结构为：

```text
Progress(
  value = $P1,
  color = $P2,
  width = $P3
)
```

对应两组实例参数：

```text
实例 A：P1=A, P2="#FF00AA00", P3=52
实例 B：P1=B, P2="#FFFF0000", P3=44
```

不过不能把所有差异都无脑参数化。否则最终会得到一个有几十个参数的“万能模板”，可读性比展开代码更差。

因此要给参数分类并设置成本：

| 差异类型 | 参数化成本 | 建议 |
|---|---:|---|
| 数据绑定路径 | 中 | 必须校验类型和数据域 |
| 文案 | 低 | 适合参数化 |
| 颜色、字号、宽高 | 低 | 适合进入视觉规格参数 |
| 素材 | 低 | 适合可选素材参数 |
| 单个可选节点 | 中 | 可使用受控条件洞 |
| 连续 children 区间 | 中 | 可使用列表洞 |
| 组件类型变化 | 高 | 仅限同类节点替换 |
| 完全不同的布局关系 | 很高 | 通常不合并 |

候选宏可以表示为：

```text
Macro {
    macroId
    rootPattern
    typedParameters
    optionalSlots
    childSlots
    expansionTree
}
```

例如：

```text
MetricRing(
    value: number-binding,
    valueText: string-binding,
    icon?: asset,
    diameter: number,
    strokeWidth: number,
    accentColor: color
)
```

展开后仍然只是：

```text
Stack
├── Progress
└── Image
```

### 参数洞必须有类型

不能设计成任意 JSON：

```text
props: object
```

而应是封闭类型：

```text
value: Binding<number>
valueText: Binding<string>
icon: Optional<Asset>
diameter: Enum<29, 38, 44, 52>
accentColor: DesignColorToken
```

这样压缩不会降低当前模板白名单的确定性。

---

## 五、相似度函数

为了从 82 棵树中找到值得比较的候选，需要定义结构相似度。

可以组合四部分：

\[
Sim(T_i,T_j)
=
w_s S_{structure}
+
w_a S_{attribute}
+
w_b S_{binding}
+
w_m S_{semantic}
\]

### 结构相似度

比较组件类型和 children 结构：

\[
S_{structure}
=
1-\frac{TED(T_i,T_j)}
{\max(|T_i|,|T_j|)}
\]

其中 `TED` 是带权 Tree Edit Distance。

编辑成本不能统一：

- 删除一个 `Text`：成本较低；
- 把 `Row` 改成 `Column`：成本中等；
- 把 `Progress` 改成 `Image`：成本高；
- 调换 children 顺序：根据组件类型决定；
- 删除动作节点：成本很高。

### 属性相似度

比较属性键集合和属性值：

- 属性名相同、值相同：1；
- 属性名相同、值不同但类型一致：可参数化；
- 属性名不同：结构差异；
- 一个是静态值，一个是数据绑定：高成本差异。

### 数据绑定相似度

不能只比较路径字符串，应比较：

```text
数据类型 + 语义角色 + 是否必需 + 所属业务域
```

例如：

```text
/batterySOC          number，主指标
/activity/steps      number，主指标
```

它们在视觉结构上可能都能进入 `MetricRing.value`，但业务模板仍必须分别保留自己的数据契约。

### 语义相似度

语义主要用于限制跨业务抽取：

- `BatteryOverview` 和 `ResourceUsageOverview` 可以共享“环形指标视觉块”；
- 但不能因为结构相似就合并成同一个业务组件；
- 共享层只负责展示结构，业务层仍分别存在。

---

## 六、候选子图发现

直接对全部节点两两计算 Tree Edit Distance 成本较高。因此先用签名分桶。

每棵子树计算粗签名：

```text
rootComponent
nodeCountBucket
depth
componentHistogram
childrenCount
bindingTypeHistogram
conditionalCount
```

例如：

```text
root=Stack
nodes=3~5
components={Stack:1, Progress:1, Image:1}
bindings={number:1, asset:1}
depth=2
```

只有签名相近的子树才进入精确比较。

推荐流程：

```text
所有子树
  -> 结构哈希：发现完全相同项
  -> MinHash/LSH：筛出组件集合相似项
  -> Tree Edit Distance：精确计算
  -> Anti-unification：生成公共模板和参数洞
```

对于当前约数千个 DOM 节点的规模，这已经足够，不需要复杂的分布式图挖掘。

---

## 七、从候选宏中选择最优集合

候选公共结构可能互相重叠：

```text
候选 A：整个 MetricRing
候选 B：MetricRing 内部的 Progress
候选 C：MetricRing + 右侧两行文字
```

不能三个都提取，否则层级会碎片化。

把问题转成带依赖的加权集合选择：

\[
\max
\sum_{m\in M} x_m \cdot Saving(m)
-
\lambda Complexity(m)
\]

约束包括：

- 同一个原始节点不能被两个同层候选重复覆盖；
- 宏之间不能形成递归环；
- 宏的参数数量不得超过上限；
- 宏至少被多个模板复用；
- 跨业务宏不能包含业务专属静态文案；
- 条件洞和 children 洞数量受限；
- 展开深度不得超过限制；
- 抽取后必须能精确还原原模板。

项目规模不大，可以用以下两种方式：

- 设计验证阶段：整数规划求全局较优解；
- 实际工程实现：按 MDL 收益排序的贪心算法，再做局部交换优化。

MDL，即最小描述长度，目标是：

\[
总代码长度
=
公共宏定义长度
+
全部模板引用长度
+
全部参数长度
\]

只要抽取后总代码更长，就不应该抽取。

---

## 八、最终形成四层组件 DAG

建议压缩后的层次是：

```text
L0 原子 Form 组件
Text / Image / Progress / Row / Column / Stack ...
        ↑
L1 跨业务视觉模式
MetricRing / IconMetric / TitleSummary / DualMetric ...
        ↑
L2 业务组件
BatteryOverview / WeatherOverview / SleepOverview ...
        ↑
L3 受控形态入口
BatteryOverviewChargingFull@1
BatteryOverviewLowCompact@1
WeatherOverviewHumidityFull@1
        ↑
L4 布局骨架
HeroActionLayout / PeerPairLayout / ...
```

这是一张有向无环图，不再是 82 份互相独立的完整树。

其中：

- L0 不动；
- L1 是图论算法从重复子树中自动发现的；
- L2 保存业务语义和数据契约；
- L3 保留当前选择、检索、尺寸及状态白名单；
- L4 保存布局容量、动作槽位和根结构约束。

---

# 在本项目 82 个高级组件上的应用过程

## 第一步：建立 82 个模板的基线清单

输入范围是：

- 72 个业务模板；
- 10 个布局模板。

每个模板记录：

```text
templateId
provider
businessGroup
cardtpl entry
DOM node count
depth
binding paths
supported shape
action requirement
expanded canonical tree hash
```

另外记录当前最终编译结果，作为以后等价性测试的黄金样本。

这里要特别注意：82 个是“受控形态数量”，不是 82 个完全不同的 DOM 结构。

## 第二步：业务内精确去重

先只在同一业务内比较。

优先顺序：

1. 电池 16 个；
2. 耳机 14 个；
3. 日历 10 个；
4. 天气 9 个；
5. 心率 8 个；
6. 其它业务。

电池中可以先识别：

```text
NormalFull
ChargingFull
LowFull
```

如果规范化后的展开树完全相同，就直接共享同一个实现。三个 ID 仍保留：

```text
BatteryOverviewNormalFull@1
  -> BatteryFullBase(state="normal")

BatteryOverviewChargingFull@1
  -> BatteryFullBase(state="charging")

BatteryOverviewLowFull@1
  -> BatteryFullBase(state="low")
```

如果当前三者连状态参数都没有影响，甚至可以映射到同一个无状态实现，只保留不同的选择元数据。

## 第三步：业务内近似抽取

接着识别：

```text
NormalCompact
ChargingCompact
LowCompact
NormalPhoneCompact
ChargingPhoneCompact
LowPhoneCompact
NormalWeatherCompact
ChargingWeatherCompact
LowWeatherCompact
```

它们很可能可以拆成两个轴：

```text
状态轴：
normal / charging / low

上下文轴：
default / phone / weather

尺寸轴：
compact
```

由原来的 9 份完整 DOM：

\[
3\times3=9
\]

压缩为：

```text
BatteryCompactBase
+ 3 个状态规格
+ 3 个上下文规格
+ 9 个薄入口
```

这里不能假定所有组合合法；合法组合仍由白名单入口明确列出。

## 第四步：跨业务发现视觉模式

业务内压缩稳定后，再跨业务寻找公共子树。

可能出现的候选包括：

```text
MetricRing
├── Progress
└── optional Image

HeroValue
├── large Text
└── unit/support Text

TitleSummary
├── title Text
└── description Text

IconMetric
├── Image
└── value Text

DualMetric
├── MetricBlock
└── MetricBlock
```

例如以下业务可能共享环形指标：

- 手机电量；
- 系统内存；
- 活动进度；
- 心率；
- 耳机电量。

但它们只共享 L1 视觉结构，不合并 L2 业务组件。

## 第五步：单独处理 10 个布局组件

布局模板的 `.cardtpl` 看起来只有一层调用：

```text
#Template HeroActionLayout@1(...children)
HeroActionLayout(children)
#End
```

真正差异位于编译器的布局校验和 lowering 规则中，而不完全位于 DOM。

因此对布局组件应构造“布局约束图”：

```text
LayoutNode {
    direction
    childRoleSequence
    minChildren
    maxChildren
    actionSlots
    sizeRules
    loweringRules
}
```

然后比较它们是否可以共享布局基类，例如：

```text
LinearLayoutBase(
    direction,
    roleSequence,
    actionPolicy,
    childBudget
)
```

但仍保留 10 个语义布局 ID。这里压缩的是实现规则，不是消灭布局类型。

`WeatherNowForecastLayout` 这种带专门业务放置规则的布局，很可能不能和普通布局彻底合并。

## 第六步：生成压缩建议，而不是直接改写

算法第一轮输出应该是报告：

```text
候选宏名称
覆盖哪些模板
覆盖哪些节点
原始总代码量
抽取后代码量
预计节省比例
参数数量
跨业务范围
风险等级
是否可精确还原
```

例如：

```text
Candidate: BatteryFullRingBlock
Occurrences: 7
Original nodes: 7 × 5 = 35
Compressed nodes: 5 + 7 references
Parameters: value, icon, color
Saving: 46%
Risk: low
Equivalence: exact
```

先人工审核候选，再决定哪些模式成为正式中粒度组件。

## 第七步：等价性验证

压缩后，每个旧模板 ID都执行：

```text
旧模板
→ 解析、展开、规范化
→ Canonical AST A

新薄入口
→ 展开公共宏
→ 规范化
→ Canonical AST B

验证 A == B
```

除了 DOM 等价，还要单独验证：

- 数据路径没有变化；
- 必需、次要、可选数据集合没有变化；
- 尺寸形态没有变化；
- 动作数量和动作位置没有变化；
- 素材参数没有被放宽；
- 最终仍只包含 10 种 Form 原子组件；
- 原有 Validator 全部通过；
- 原模板 ID 仍能被现有检索和选择流程使用。

若存在有意视觉变化，则不能算“无损压缩”，必须作为单独方案处理。

---

## 对这 82 个组件的预期结论

根据目前看到的文件结构，我预计：

- 82 个受控 ID 大部分应该保留；
- 72 个业务模板的完整 DOM 实现可以明显减少；
- 最先能压缩的是电池、耳机、日历和心率；
- 10 个布局 ID 应保留，但底层实现和约束声明可以部分统一；
- 最终可能形成约 10–20 个跨业务视觉模式，加约 11 个业务层组件，再由 82 个薄入口描述合法形态；
- 最终 genui 仍全部展开为原子组件，所以端侧协议完全不受影响。

一句话说，目标不是做传统的“文件压缩”，而是从 82 棵重复 DOM Tree 中自动学习一套**带类型参数、带契约、可无损展开的组件 DAG**。图论负责发现公共结构和选择最优子图；项目规则负责决定哪些差异允许变成参数，哪些必须继续保留为独立形态。