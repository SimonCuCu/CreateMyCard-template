`T_i` 就是第 `i` 个模板经过解析、规范化后的 DOM Tree。

例如：

```text
T_normal_full
= BatteryOverviewNormalFull@1 的树

T_normal_hero
= BatteryOverviewNormalHero@1 的树
```

它们不是源文件文本，而是去掉排版差异、统一属性顺序后的树结构。

## 1. 两个电池模板的树是什么样

简化后，`NormalFull` 大致是：

```text
Column
├── Text("设备电量", subtitle)
├── Text("电量 ${percentText}，${level}，${charging}", body)
└── Stack
    └── Stack
        ├── Progress(value=percent, type=ring, 52×52, ...)
        └── IfPresent(batteryIcon)
            └── Image(batteryIcon, 24×24)
```

`NormalHero` 大致是：

```text
Column
└── Column
    ├── Stack
    │   ├── Progress(value=percent, type=ring, 52×52, ...)
    │   └── IfPresent(batteryIcon)
    │       └── Image(batteryIcon, 24×24)
    └── Column
        └── Text(level, body)
```

两棵完整树的视觉用途不同：

- `NormalFull`：标题、摘要、底部环形指标；
- `NormalHero`：居中的环形主视觉，加一行状态说明。

所以不能期待“整棵树完全合并”。但两者内部的环形指标子树是同一个候选：

```text
Stack
├── Progress
└── IfPresent(icon)
    └── Image
```

图论算法要找的通常就是这个公共子树，而不是强行把两个根模板合并。

---

## 2. TED 是怎么计算的

`TED(T_i, T_j)` 是 Tree Edit Distance，树编辑距离。它表示：把树 `T_i` 变成树 `T_j`，最少需要多少次、总代价多大的编辑操作。

允许操作通常是：

- 删除节点；
- 插入节点；
- 把一个节点替换成另一个节点；
- 修改一个节点属性。

这里必须使用“带 UI 语义权重”的 TED，不能用所有操作代价都为 1 的普通版本。

一个可行的代价表：

| 操作 | 例子 | 代价 |
|---|---|---:|
| 相同组件、相同属性 | 两个同样的 `Progress` | 0 |
| 同组件、颜色不同 | `#FFF9A01E` → `#FFFF0000` | 0.2 |
| 同组件、尺寸不同 | `52×52` → `44×44` | 0.3 |
| 同组件、绑定路径不同但类型相同 | 电量百分比 → 内存百分比 | 0.5 |
| 必填绑定变可选绑定 | 必须显示 → 可缺省 | 0.8 |
| 文本静态标签变成数据绑定 | `"设备电量"` → `${level}` | 1.5 |
| 插入/删除普通 Text | 增减一行辅助文案 | 1 |
| 插入/删除条件节点 | 增减 `IfPresent` | 2 |
| 插入/删除布局容器 | `Column`、`Stack` | 2.5 |
| `Row` 改 `Column` | 主轴方向改变 | 3 |
| `Progress` 改 `Text` | 指标图形变普通文本 | 禁止或极高代价 |

根本原则是：**不能让算法用一次廉价“删除容器”掩盖真实布局变化。**

### 这两个模板的 TED 怎么看

先匹配根：

```text
Column  ↔ Column
```

组件类型相同，所以结构替换代价为 0；但根容器样式不同：

```text
NormalFull: justifyContent = spaceBetween
NormalHero: justifyContent = start
```

这属于属性差异，可能是 `0.2 ~ 0.5`。

再看子节点：

```text
NormalFull:
  Text(title)
  Text(summary)
  Stack(ring)

NormalHero:
  Column(
    Stack(ring),
    Column(Text(level))
  )
```

这里会出现：

- `NormalFull` 的标题行，Hero 中没有对应节点：删除代价；
- `NormalFull` 的摘要行，不能直接等价成 Hero 的 `level`：高属性差异或删除+插入；
- Hero 多了一层 `Column`：插入布局容器代价；
- Hero 的 `level` 文本：插入代价；
- 两者内部 `Stack(ring)`：可精确匹配，代价为 0。

所以，完整根树的 TED 不会很小。算法应得出：

```text
完整模板：
NormalFull 和 NormalHero 不是理想的合并对象。

内部子树：
两者的 RingBlock 是理想的精确复用对象。
```

这也是为什么算法不能只算：

```text
TED(Template A, Template B)
```

而要对所有候选子树计算：

```text
TED(Subtree(A, u), Subtree(B, v))
```

其中 `u`、`v` 是两个模板中任意节点。真正被抽取的是公共子树，而不是整个模板。

---

## 3. TED 的动态规划过程

对两个节点 `u`、`v`，先算节点替换代价：

\[
replace(u,v)
=
typeCost(u,v)+attributeDistance(u,v)
\]

再算 children 的最小编辑代价。

如果 `u`、`v` 都是有序容器，其 children 类似两个序列：

```text
u.children = [a_1, a_2, ..., a_m]
v.children = [b_1, b_2, ..., b_n]
```

用动态规划：

\[
D[p,q]
=
\min
\begin{cases}
D[p-1,q] + delete(a_p)\\
D[p,q-1] + insert(b_q)\\
D[p-1,q-1] + TED(a_p,b_q)
\end{cases}
\]

最后：

\[
TED(u,v)=replace(u,v)+D[m,n]
\]

完整实现可用 Zhang–Shasha 或 APTED，但项目里应额外加上：

- 类型兼容矩阵；
- UI 容器高删除成本；
- children 顺序不可随意交换；
- 绑定类型校验；
- 条件节点和动作节点的特殊成本；
- 最大树深度与最大候选规模。

这些全部是确定性脚本，不需要模型。

---

## 4. 属性相似度怎么计算

属性相似度不是把 JSON 直接字符串比较，而是拆成四层。

\[
S_{attr}
=
w_kS_{key}
+
w_tS_{type}
+
w_vS_{value}
+
w_cS_{contract}
\]

### 属性键相似度

先比较两个节点有哪些属性。

例如两个 `Progress` 都有：

```text
value
total
type
color
backgroundColor
width
height
strokeWidth
```

那么键集合几乎一致：

\[
S_{key}=
\frac{|keys_i \cap keys_j|}
{|keys_i \cup keys_j|}
\]

这部分接近 1。

### 属性类型相似度

同为 `number`、同为 `color`、同为 `Binding<number>`，说明可参数化。

例如：

```text
value = Bind("/batterySOC")
value = Bind("/memory/usagePercent")
```

路径不同，但都是数值绑定，可作为 `MetricRing.value` 的参数。

相反：

```text
value = Bind("/batterySOC")
value = "正在充电"
```

一个是数值，一个是文本，不可视为同类参数。

### 属性值相似度

对于可参数化值，比较的是“差异成本”，不是相等与否。

```text
color="#FFF9A01E"
color="#FFFF0000"
```

可变为：

```text
accentColor: ColorToken
```

差异低。

而：

```text
content="设备电量"
content=Bind("/batteryCapacityLevelDesc")
```

是“固定标题”与“动态数据内容”的语义变化，不应直接抽成一个普通 `content` 参数，代价高。

### 契约相似度

检查参数化后是否仍然满足已有约束：

- 数据类型一致；
- 必需数据仍是必需；
- 可选数据不会被误变成必需；
- 素材仍只能由允许素材传入；
- 动作的数量和位置不变；
- 不会产生 Form 协议外属性。

这一层是压缩是否允许落地的硬门槛，不只是相似度分数。

---

## 5. “语义相似度”不是让模型猜

最稳妥的方案中，语义相似度应以**规则和结构化标签为主**，模型只可选地用于召回候选。

每个节点或子树提取一个语义签名：

```text
SemanticSignature {
  renderRole: "metric-ring" | "title" | "summary" | "action" | ...
  componentKinds: {Stack, Progress, Image}
  bindingTypes: {number, asset}
  bindingRoles: {primaryMetric, optionalIcon}
  displayRole: "hero" | "support" | "metadata"
  interactionRole: "passive" | "action"
  businessDomain: "battery" | "health" | "system" | ...
  dataRequiredness: required | optional
}
```

其中绝大多数都能确定性获得：

| 信息 | 来源 | 是否需要模型 |
|---|---|---|
| 组件类型、父子关系 | `.cardtpl` AST | 否 |
| `Progress` 是环形指标 | `type="ring"` | 否 |
| 数据类型 | Schema | 否 |
| 必填/可选 | `primaryData`、`secondaryData`、`optionalData` | 否 |
| 动作/非动作 | 节点类型和事件声明 | 否 |
| Hero/Support | 模板形态和父布局位置 | 否 |
| 业务域 | Provider、业务组 | 否 |
| 文案含义 | 静态文本或模型辅助 | 可选 |

然后再算：

\[
S_{semantic}
=
0.35S_{renderRole}
+
0.30S_{bindingRole}
+
0.20S_{interactionRole}
+
0.15S_{domain}
\]

这里的权重只是起点，实际要靠样本调参。

### 电池 Full 与 Hero 的语义相似度

对完整根树：

| 维度 | 结果 | 原因 |
|---|---:|---|
| 业务域 | 高 | 都是 BatteryOverview |
| 数据集合 | 高 | 都使用百分比、状态、等级等电池字段 |
| 展示角色 | 中低 | 一个是摘要卡，一个是主视觉卡 |
| 交互角色 | 高 | 都是展示，无动作 |
| 布局语义 | 低 | 一个底部指标，一个居中 Hero |

所以完整树的语义相似度可以“中等”，但不会高到允许直接合并。

对内部 RingBlock：

| 维度 | 结果 |
|---|---:|
| 渲染角色 | 完全一致：ring metric |
| 数据类型 | 完全一致：数值 |
| 数据角色 | 都是主指标 |
| 图标 | 都是可选素材 |
| 交互 | 都是被动展示 |
| DOM 结构 | 完全一致 |

因此 RingBlock 的语义相似度和结构相似度都接近 1，是可靠的抽取对象。

---

## 6. 语义相似度在算法里只能“放行”，不能“决定改写”

这点很关键。

不应因为：

```text
BatteryOverview 与 ResourceUsageOverview 都有“百分比”
```

就让模型自动把两个组件合并。

正确流程是：

```text
语义相似度
  -> 判断两个子树是否值得进入精确比较候选池
  -> 计算带类型约束的 TED
  -> 反统一生成参数化公共树
  -> 检查数据契约、动作、尺寸、协议
  -> MDL 收益大于零
  -> 才允许抽取
```

即：

```text
语义相似 ≠ 可以合并
语义相似 + 结构可统一 + 契约不变 + 代码收益 > 0
= 可以抽取公共组件
```

---

## 7. 哪些是脚本，哪些可以用模型

建议第一版全部关键决策都由脚本完成。

| 工作 | 脚本 | 模型 |
|---|---|---|
| 解析 cardtpl / JSON | 必须 | 不用 |
| 建树、规范化、哈希 | 必须 | 不用 |
| 精确公共子树 | 必须 | 不用 |
| TED / 属性距离 | 必须 | 不用 |
| 反统一、生成参数洞 | 必须 | 不用 |
| 数据类型、可选性、动作校验 | 必须 | 不用 |
| MDL 收益与候选选择 | 必须 | 不用 |
| 展开后等价性验证 | 必须 | 不用 |
| 给公共组件起可读名字 | 可选 | 可用 |
| 对跨业务候选做初步推荐 | 可选 | 可用 |
| 从静态文案中补充语义标签 | 可选 | 可用，但必须人工/规则校验 |

模型不能参与的事情包括：

- 判定两个模板是否等价；
- 自由选择参数类型；
- 决定删掉数据字段；
- 生成未经验证的新组件结构；
- 放宽模板、素材、动作或数据约束。

更实际的做法是：模型只说“`BatteryRing` 和 `MemoryRing` 可能相似”；脚本负责确认它们的 DOM、数据类型、参数洞和展开结果是否真的满足规则。